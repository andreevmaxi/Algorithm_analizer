/*
#include "KorobkaManager.h"

int main(int argc, char** argv) {
	Color MainWinColor = { 0,0,0 };
	char MainWinName[6] = "paint";
	int hieght = 1080;
	int width = 1920;
	WindowsTree* MainWindow = 
		KorobkaInit(argc, argv, MainWinColor, MainWinName, hieght, width);

    while (!MainWindow->ShouldClose())
    {
		MainWindow->Draw();
		MainWindow->ProcedureEvents();
    }
}
*/